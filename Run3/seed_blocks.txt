# Which seed block each submitter owns.
#
#   SeedBase is allocated inside your own block, tasks 20,000 apart.
#   block N = N*10,000,000 .. +10,000,000, so 500 tasks per person.
#   Blocks never overlap, so two people cannot collide.
#
# Block 0 is what was allocated before 2026-09-15. Block 80 and up is reserved
# for hand-written configs. To add yourself, append the next free number.
#
# <username> <block>
pelai 1
fanx 2
